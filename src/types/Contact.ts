// Defines the shape of a contact throughout the app
export interface Contact {
  id: string;               // Unique ID for FlatList key
  name: string;             // Full name
  phone: string;            // Phone number
  address: string;          // Mailing address
}
